//history is a package installed automatically with react-router-dom
import { createBrowserHistory } from "history"

//createHistory is a function which we call to create new history object
export default createBrowserHistory()